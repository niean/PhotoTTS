# 产品需求 - 迭代演进

## 约束
本文是人工定义原始的产品需求，不是AI开发完成后的记录。

## 需求列表
[260207]
- 新增
    - 音频：播放音频时，对应的图片自动全屏。具体的，
        - 点击播放语音按钮时，语音对应的图片、自动展开为全屏模式
        - 语音播放完毕后，当前图片退出全屏
- 优化
    - 记录：音频播放完成后，不再自动跳出记录保存提示框
    - 状态：状态区域默认不展示，可通过配置文件、自定义开启展示

[260208]
- 优化
    - 音频：播放音频时的全屏图片，全不透明、盖住顶导
    - 状态：状态区域，从主页面、改为弹出框，
        - 出错时，自动弹出
        - 处理时，是否展示进度？

[260209]
- 新增
    - 拍照：简化UI，干掉拍照专用按钮，改用首页图标、缩略图按钮
    - 状态：状态区域，展示形式调整为`图片预览`区域之上的弹出层，展示时机是在`OCR+TTS处理过程中`
        - 处理结束时，如果结果成功、则自动隐藏，结果失败、则持续展示直至人工点击退出；
        - 状态区域，应复用原来内联形式的页面组件，避免从头重写。
- 优化
    - 相机：相机页面，底部不要漏出底色(全屏)，支持左侧边滑动返回
        - TODO：选择图片后，相机顶部或底部偶尔漏出底色、功能不稳定
    - 图片：全屏图片，因尺寸比例不match屏幕、fit后留下空白区域，空白区域使用图片特征、模糊填充
        - 建议把原图做高斯模糊 + 拉伸1.15x，作为背景填充空白
    - 记录：记录查看，改为标准的右进左出。顶导风格，应该跟主页保持一致，如向左的按钮等
        - 为避免手势识别重复，将记录详情独立拆分文件

[260210]
- 优化
    - 记录：记录列表，去掉日期压缩行高
    - 记录：记录列表，每条记录增加头像，默认取记录中的第1张作为头像、支持自定义
    - 记录：记录编辑，页面改为右入左出、独立拆分文件
    - 记录：记录保存，record.json中不存储音频数据
    - 记录：记录导入，支持导入某1个记录
    - 记录：记录保存、编辑、查看，三类页合为一个文件
        - 保存、编辑页面，允许变更名称、头像，记录其它内容在之后展示；
        - 查看页面，名称、头像放在最前，不允许编辑

[260211]
- 优化
    - 播放：整合操作按钮、缩略图按钮、缩略图，形成新的播放控制区
        - 本次调整，只改变UI布局、不要改变执行逻辑；
        - 播放按钮，放到缩略图、缩略图按钮之前，按钮风格保持一惯性；
        - 更多按钮，图标采用`省略号`，放到播放按钮之后、缩略图之前；
            - 播放按钮、更多按钮，不允许横向滑动；
        - 二级菜单，点击更多按钮、弹出二级菜单，折叠识别、删除、拍照、保存会话记录操作，操作名称前加图标、操作间加分割线；
        - 缩略图，当前图片超出屏幕后，应自动滚动、使之可见
- 新增
    - 底导：引入底导，功能簇包括首页、记录、调试、我的四个功能
        - 底导，图标改为空心，Tab切换时去掉液态效果(未实现，推测是SwiftUI默认设置)
        - 首页Tab，修改为 AppPageView(原APP介绍页)
        - 记录Tab，移除左上角的介绍页按钮，移除右上角的调试日志、配置按钮
        - 底导，适配全屏模式，全屏大图、摄像机等应该遮盖底导

[260212]
- 新增
    - 我的：参考微信APP的`我`页面布局，优化`我的`Tab页。左上角，使用APP介绍页头像；页面菜单，包括设置、关于(即介绍页)，后续还会扩展
        - 关于，点击按钮、弹出介绍页，支持顶导、左上角返回图标，支持手势识别，风格和既有页面保持一致(如会话记录列表页)；
            - 还原、保存，改为顶导右上角图标按钮；
        - 播放历史：新增播放历史，以一个新的按钮分组、放到现有分组(包含配置和关于)下面。播放历史，记录哪天的什么时间、听了什么绘本(按名称去重 保留最后一次播放信息)，并以纵向时间线展示；播放历史，放在一个大json文件即可，支持导入、导出；
            - 播放历史，如果记录没有名称，则剔除之；
        - 制作历史：参考播放历史、新增制作历史，放到播放历史按钮的下方(同分组)。制作历史，记录哪天的什么时间、保存了什么绘本，并以纵向时间线展示；制作历史，放在一个大json文件即可，支持导入、导出，规则和播放历史对齐即可
    - 首页：定制首页，上半段为制作入口、下半段为会话记录列表
        - 首页，应遵循风格一致的顶导设计；
        - 制作入口，左侧为拍照、右侧为选图，图标风格参考`图片处理`入口页；受纵向滚动控制；
        - 会话记录，复用现有的会话记录视图，不要创造新东西；
    - 记录：图片处理页ContentView，根据不同操作、设置不同的导航标题
        - 加载记录后，标题设置为播放；
        - 拍照或选图后，标题设置为制作；
        - 其它状态，标题设置为制作
- 优化
    - 治理：去掉介绍页面
    - 记录：改善操作的连贯性
        - 从首页加载记录后，自动触发播放；播放完成后，自动回到首页，且清空播放状态；
        - 从首页发起拍照后，如果拍照记录为空、则应该返回首页(而不是停留在记录页)；
        - 从首页发起选图后，如果选图记录为空或取消，则应该返回首页；
        - 图片处理页，制作时允许变更操作，包括识别、删除、保存、拍照、缩略图调整顺序、缩略图删除；其它状态不允许变更操作；
        - 图片处理页，打开历史记录、改为选图制作，图标和名称跟首页对齐；删除顶导右上角按钮，不再允许从图片处理页加载记录；
        - 图片处理页，删除语音开关；
    - 分享：记录导出、导入时，不要降低图片精度，按照此原则、检查并修复现有代码

[260213]
- 新增
    - 消息：调试Tab，改名为消息，包括分析(播放 制作)、日志(调试 更新)功能区
        - 消息Tab布局参考`我的`， 图标参考`微信`Tab图标；
        - 调试日志，独立成一个按钮分组；
        - 把播放历史、制作历史，迁移到`消息`Tab下，独立一个分组；
        - 增加`更新记录`按钮，放到调试日志所在的分组；更新记录页，独立文件，内容来自Resources/changelogs.md，支持简单的md渲染(含表格)
    - 播放：独立出播放视图，用作默认播放器，超脱于底导Tab
        - 播放视图，点击事件不再导致全屏图片退出全屏、改为操作按钮的展示和隐藏控制；
        - 播放视图，起始状态下不展示操作按钮；
        - 播放视图，如5秒内没有点击操作按钮、也没有点击事件，则自动隐藏操作按钮；
        - 播放视图，暂停，不退出播放；增加`退出`操作按钮，点击后退出播放(即退出全屏)，退出按钮使用典型图标；
        - 从首页触发的播放记录，使用播放视图，且不再跳到制作Tab；
    - 制作：记录Tab改名为制作，拆分独立文件、重命名视图为MakeView
        - 制作页，OCR+TTS完成后，使用PlayView全屏播放、不再使用自定义的播放器；
        - 制作页，大图不再允许全屏，删除全屏对应的代码(如状态控制)；点击大图，页不要再触发播放；
        - 制作页，不再支持加载记录，删除对应的代码逻辑；
        - 制作页，不再支持从首页点播放记录、到制作页，删除对应的代码逻辑；
        - 制作页，不再自定义播放功能，删除对应的代码逻辑；

- 优化
    - 底导：切换新Tab后重置状态，包括首页、消息、我的三个Tab，状态包括页面、状态数据等
    - 消息：调试日志，右上角按钮、从图标改为文字
    - 制作：制作页只关注制作，据此调整按钮样式
        - 制作页，顶导右上角增加更多按钮，采用蓝色加号图标(类似微信右上角的加号)，点击后弹出二级菜单，二级菜单包括拍照、保存、删除三个操作按钮，样式和行为跟之前的二级菜单保持一致；
        - 制作页，缩略图按钮，删除更多按钮(即老的二级菜单)，取而代之的是识别按钮，图标风格延续之前即可(绿色、加粗)；
    - 记录：记录查看，图片预览加载所有照片、而不只是第一张，降低分辨率提升性能
    - 我的：增加一个分组，新增`记录管理`按钮，点击打开标准的记录列表页面；此处的记录列表不允许播放
        - 记录：合并会话记录的管理状态、到一个state，支持标准、嵌入、管理三种模式。标准模式，允许所有功能；嵌入模式，不支持顶导，只允许播放、查看操作；管理模式，支持顶导，允许查看、编辑、删除操作(不允许播放)
    - iPad：适配iPad，底导跑掉了最顶端、修正下
        - iPad上，底导宽度太窄了，修正下（未实现，因为iPadOS强限制）
        - 修改Info.plist，只允许竖屏、不允许横屏，声明APP需要全屏

[260214]
- 优化
    - 背景：自定义ZStask组件，统一底色等样式
    - 记录：记录列表，横向两侧漏出了底部
    - 消息：播放历史、制作历史优化
        - 播放历史，自动过滤掉 名称中包含`未命名`、`测试`字样的记录，保存、展示时均过滤；
        - 制作历史，自动过滤掉 名称中包含`未命名`、`测试`字样的记录，保存、展示时均过滤；
        - 播放历史，保存每次播放记录、不再按名称去重；读取展示时，按名称去重并、计数，按最近一次播放时间、倒序排列；如果多次播放，记录播放次数、并展示在名称后面形如`(3)`
    - 全屏：全屏模式，去掉状态栏
    - 全屏：启动页使用全屏容器；同时，关于页面维持现状(非全屏)
    - 我的：关于，文字布局有问题、和相机图标距离过远
    - 制作，识别过程中只能看到状态页，看不到图片、缩略图等了（结论：状态页背景色问题）
    - 制作，缩略图背景色不协调（结论：缩略图背景色问题）
- 治理
    - 组件：抽象公共组件顶导、顶导+左导，修改现有代码
        - 顶导，按照左侧按钮、中部标题、右侧按钮的布局，提升顶导组件的抽象程度；
        - 顶导、左侧手势识别(左导)，两者进一步抽象、为一个组件TopAndLeftSideNavigationBar；

[260215]
- 治理
    - 性能：优化内存占用，防止被OS杀死
        - 调试日志，每次只往内存中加载最新的50条，而不是加载所有；
        - 会话管理，会话记录列表页内存消耗严重，优化下(结论：头像图片太大了、缩小即可)；
        - 记录查看，图片预览改为按需加载，而不是全部加载；
        - 记录播放，存在内存突增，请只加载本帧，而不是全部加载；
            - 切换图片时屏幕闪动，改为加载本帧、缓存下一帧，牺牲1倍内存、换取视觉舒适
- 优化
    - 记录：加载时，提示Loading、而不是无响应（内存优化后有极大缓解，放弃需求）

[260218]
- 优化
    - 制作：记录管理，新增操作按钮、允许加载记录到制作，以便重新进行OCR+TTS、进而制作
        - OCR：断句问题（基模从1.6升级到2.0后 问题解决）
    - 记录：记录头像，从临时生成、改为提前生成好，并保存到记录中；不要放到record.json中，使用时从磁盘加载。对应的，更新图标时、也要生成好
        - 执行一次性任务，为存量记录、预生成头像图标
    - 记录：图片大小设置上限，避免IOSurface分配不出内存空间
        - 执行一次性任务，修复所有记录图片大小，如果超出Constants中saveImageMaxPixel、则重新计算后保存
            - 被缩小图片的记录，重新计算记录存储空间大小等元数据，并更新到元数据中
            - 重新计算、更新元数据，包括metadata.json、record.json
        - 所有加载到APP中的图片，都应满足图片大小限制(saveImageMaxPixel)，包括选图、拍照等，不满足时则缩小图片

[260226]
- 优化
    - 治理：抽象 项目通用的Harness Engineering风格AI知识库
        - 按照Harness Engineering风格，帮我完善文档库 context/
        - 按照Harness Engineering风格，日常的小产品功能迭代、应该放在哪个文档来描述？
        - 按照Harness Engineering风格，设置AGENT.md为AI入口
        - 按照Harness Engineering风格，应该有PRODUCT_SENSE.md文件、定义产品设计的核心理念与判断准则
        - 按照Harness Engineering风格，合理拆分、合并doc/01-prd-summary.md 和 PRODUCT_SENSE.md两份文档的内容，避免重复
        - 按照Harness Engineering风格，补全项目在质量、安全上的规范
        - 按照Harness Engineering风格，结合prd-specs历史，补全内存性能规范
        - 参考OpenAI提出的Harness Engineering风格，结合文档库现状，帮我抽象一份AI上下文知识库 的管理规范、放到AGENT.md文档中
        - 最后检查：本项目抽象了 通用的Harness Engineering风格AI知识库，请帮我检查下文档库、还有哪些严重问题
    - 治理：清理未使用的代码
        - 请扫描代码、列出具体的废弃代码清单，待我确认后、再执行删除
    - 记录：记录查看，名称区域除了展示名称，还应该展示UID
    - 记录：记录导入，按照记录ID去重，如遇相同ID、则不导入
        - 导入统计，也要体现出共计多少、导入多少、ID重复多少
        - 记录导入后，应更新它的元数据，如磁盘空间占用
    - 记录：记录导出，Sessions下的每个记录文件夹，名称从记录UID、改为记录名称
        - 先对记录名称做合法性校验、替换，防止新建文件夹因命名问题而失败
    - 记录：导入导出，修改弹框风格，使跟播放历史保持一致
        - 问题：记录管理，导出按钮，点击后会先闪现全局灰底色、然后才会弹出文件选择框，请去掉闪现的全局灰底色
        - 迭代：记录管理，清空按钮 作为二级菜单、放到更多按钮下；更多按钮，放到导入、导出的右侧
        - 迭代：记录管理，导入、导出按钮 也放到`更多`二级菜单中，二级菜单风格跟制作页的保持一致
            - 迭代：记录管理，更多按钮的图标，从点号、换为加号
- 新增
    - 记录：记录导出，单条记录支持导出操作，把导出当做Edit操作
        - 迭代：单条记录导出，导出目录应该以记录名称来命名，跟全量导出时单个记录目录的命名习惯保持一致

[260227]
- 新增
    - 播放：新增语音控制播放的功能，控制指令如 `嘿 Siri，播放绘本 "贝贝熊-作业的烦恼"`，复用之前的播放页面
        - 问题：iPhone上执行语音`在PhotoTTS中 播放绘本`时，Siri反馈`我没看到相关APP`
            - 追加：早晨编译安装时，测试OK；当天晚上无法使用，Siri提示`我没看到相关APP`。早上测试OK，晚上不可用，这是什么原因？
        - 迭代：控制指令，除了`在PhotoTTS中播放绘本`，还希望支持简单的 `播放绘本`（结论：Apple开发规范不允许，放弃）

[260303]
- 优化
    - 治理：日志打印使用组件，禁用print
    - 治理：修改home默认图片，避免开发者隐私泄露
    - 治理：项目中的Tests代码，足够新吗、还有效吗（结论是全部过时）
    - 治理：结构化抽象Skills
    - 治理：生成技能：Skill回填，Subagent回填。命名要求以 `回填-`开头儿。
    - 治理：功能迭代，中间过程落盘到临时文件context/agents/tmp-spec-${事项}.md，任务结束后删除临时文件。请按照此要求，更新Skill及其它相关规范。

[待办]
- 优化
    - 记录：记录列表，分页懒加载、每页最多20个，需要覆盖到首页、记录管理等所有记录列表页
    - 其它：日志中的会话记录标志，从ID(如3545302D-5749-4295-8DF2-518DF07250A8)改为表意的名称(如26.03.02 贝贝熊-X)
- 新增
    - 制作：TTS，配置文件支持选择TTS音色
    - 制作：OCR，配置文件支持选择OCR模型
    - 整体：上架到AppStore
    - 制作：拍照前先生成记录，防止图片遗失
    - 播放：图片生视频，使绘本故事更具视觉连续性


