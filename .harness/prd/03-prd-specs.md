<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260405]
- 优化
    - 首页：FR，展示日期，未播放过的记录也展示日期，播放次数不展示(即没有/N)
        - 要求：MM.dd是名称前缀中的日期，不管有没有播放过
    - 制作：拍照，点返回按钮后、会卡顿在相机页面一会儿，希望是跳到制作页展示Loading、与`记录再次新制作`的风格保持一样
    - 制作：ISSUE，制作过程中切到了首页、在完成前切回了制作Tab，制作完成后不会跳转记录编辑页，制作过程中如果不切底部Tab、制作完成后能够正确跳转记录编辑页

[20260408]
- 优化
    - 我的：阅读报告，在每周/每月之外，新增`半年`选项
    - 我的：阅读报告，`连续听读`天数改为 `听读天数`，不再要求连续
        - 迭代：`听读天数`，统计周期不再定死最近30天，而是Follow每周/每月/半年选项

[20260409]
- 新增
    - 我的：阅读报告，新增`阅读统计`指标区，直方图展示 ①天级听书本数(默认天级、可切换至周/月)，②小时时段听书本数(每天按小时跨度切分成段，展示每一段的播放本书，如20:00-21:00)
    - 我的：阅读报告，移除顶部的`听书天数`，放到数据指标卡片、作为第3个卡片

[20260410]
- 优化
    - 首页：卡片，封面图片width:height设置为16:9，根据屏幕宽度+每行卡片数、计算出封面图片width，再计算height=9/16*width，同时适配iPhone/iPad
    - 首页：卡片，封面图片预生成(类似记录头像)，封面图片只允许是第1张，编辑时支持旋转图片方向、支持按coverAspectRatio比例做裁剪
        - 迭代：Bug，按coverAspectRatio比例做裁剪 不好使，裁剪区无法移动；
        - 迭代：裁剪时，无法缩放；裁剪后，无法保存(点确定按钮无反应)；
        - 迭代：裁剪后，保存成功后、封面预览还是老图，需要退出再进入才能看到变更后的效果；
        - 迭代：记录头像、封面图片，两者合并为一个块，命名为头像和封面，头像在左、封面在右；点击图片进入编辑，记录头像编辑时维持既有长宽比，允许旋转(逆时针)、允许裁剪，跟封面图片编辑保持一致
        - 迭代：头像，编辑后未真正保存；
        - 迭代：头像和封面，位置调整，放到`名称`下方、`播放方向`上方；
        - 迭代：封面图片裁剪页，iPhone上尺寸合适，但iPad上尺寸过小；
        - 迭代：旋转，按钮图标是逆时针、但真实是顺时针，请将旋转改为逆时针；

[20260411]
- 优化
    - 管理：记录编辑完成后返回列表，回到进入编辑页前的位置、而不是初始状态
    - 管理：传输，全选、反选之外，再增加一个`本页`选项，放到全选之后、反选之前
    - 首页：搜索，搜索栏左侧结构化，支持选系列(系列选项提前生成好)，默认不设置
        - 迭代：系列筛选，UI样式优化，内嵌在搜索框内(最左侧)、而不是独立菜单
    - 管理：搜索，搜索框支持筛选系列，跟首页对齐、要求复用搜索组件
        - 迭代：搜索框，未指定系列时的展示值，从不限、改为系列
        - 迭代：搜索框，未指定系列时的展示值`系列`，效果等同于不限，但并不是把菜单选项中的不限也修改为系列
    - 我的：调试日志，要求开启详情；目前，日志不展示TTS请求失败、不符合预期
    - 制作：未完成记录(如OCR、TTS失败等)，要支持保存；未完成的记录加特殊标记，不允许播放，允许查看&编辑&制作等

[20260412]
- 优化
    - 制作：制作过程中，如果未切其它底部Tab、就不会保存临时记录，期望不切Tab也要保持临时记录
    - 管理：多选记录时，展示更多按钮(沿用加号图标)，更多菜单包括导出、删除，对应的`导出`不再展示在顶导
- 新增
    - 首页：调整记录列表顺序，将"最早日期的未播放列表(且仅最早的一天)"、放到记录列表最上方，卡片底色特别是卡片底部文字底色、调整为浅黄色(表征为待办事项)；其它记录维持现有的排序、UI样式
        - 要求：抽象首页的播放计划，①含未播放记录的最早的日期，②该日期下的未播放记录是待播放的计划列表，③计划列表要单独设置UI样式、已做区分；播放计划之外的记录，维持既有实现 —— 记录只调整顺序、不重复
        - 追问：今日待听，和记录分页是什么关系？我希望今日待听是全局记录的一部分、接收分页规则管理，也不要切分今日待听、全部记录两个块
    - 首页：播放计划，每天只有一个待办日期，听完这个日期、也不要切换新的待办日期了


---

[待办]
- 优化
    - 制作：TTS失败后，允许单独重试(复用已有的OCR结果)
    - 制作：Bug，选图，点击确认后，页面会先跳到顶端、然后再进入制作页
    - 制作：制作任务支持并发，OCR识别只有要串行（受限于OCR API并发限制）
    - 播放：播放器，做成独立页面，方便从管理Tab按系列播放
    - 首页：连续播放，仅限播放计划内的记录，非播放计划的记录不连播
    - 管理：允许替换特定照片
    - 首页：卡片，封面支持上传自定义图片
    - 首页：当前的布局，导致历史记录很难再次被发现
- 新增
    - 我的：时长管控，家长可设置单次/每日最长使用时长，到时间后温柔提醒"今天的故事时间结束了"并自动暂停
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 管理：分组，进化为可视化书架；
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 需求：多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 问题：新项目，缺少HE初始化引导，第一次任务完成时能自动回填 => 引入 Skill`bootstraper`，用于项目知识的初始化，兼容常用IDE
        - 引入 Skill`hr`，用于项目角色的初始化；
        - 用一个小任务，Trigger项目初始化；
    - 规则：引入 Skill`ship`，用于Ship环境测试
    - 规则：其它优化
        - 为coder和tester，为故事Story创建一个MVP、快速Run起来
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；
- 备选
    - 管理：FR，表头加一个Summay，展示绘本总数、系列数、播放数等；导出时隐藏
    - 我的：配置修改，从Json文本升级为产品功能
    - 播放：图片生视频，使绘本故事更具视觉连续性
    - 制作：TTS，字数太多调用失败，音频分段
