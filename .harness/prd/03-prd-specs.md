<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260420]
- 优化
    - 启动：启动，在启动动画的同时，预加载首页卡片列表；现状是，首页卡片偶发先展示空、然后渲染出封面
    - 首页：卡片列表，从分页、改为单页懒加载，懒加载长度复用现在的分页长度

[20260421]
- 优化
    - 首页：左上角增加排序方式切换按钮，样式和管理左上角按钮保持一致，支持列表、系列两种方式，默认列表方式、按日期+系列+名称排序，系列方式、按系列+日期+名称排序
        - 迭代：首页排序，除列表模式外、无需考虑播放计划对排序的影响

[20260425]
- 优化
    - 首页：播放列表，如开启播放计划、则不展示播放计划日期之后的记录
    - 管理：记录列表，记录行 在音频大小之后、展示播放次数，未播放则不展示该项
    - 管理：记录列表，从分为改为懒加载，懒加载使用现有的翻页阈值(类似首页的做法)
    - 管理：分组模式，新增`未读和已读`分组、切换顺序放到月份分组之后，未读在前、已读在后

---

[待办]
- 优化
    - 管理：顶导右侧更多按钮，拍照制作、选图制作间增加分割线(类似导入和导出之间)
    - 传输：传输记录，一个一个的传输，即使中途断点也可部分记录传输成功
    - 管理：允许更新记录的照片，包括头像、封面、普通图片等
    - 我的：播放设置，播放计划Sector的今日状态，如果未完成、给出播放时长预估，如果已完成、给出播放时长
    - 迭代：做一次性任务，给20260302及更早的未读绘本、补充一次播放记录，播放时间统一设定为 `20260302 20:30:00`对应的时刻
- 新增
    - 制作：要点图片，改进为要点视频
    - 重构：首页->播放，干掉制作Tab、制作页蜕化为无状态的二级子页面
- 备选
    - 管理：表头加一个Summay，展示绘本总数、系列数、播放数等；导出时隐藏
    - 管理：分组，进化为可视化书架；
    - 播放：图片生视频，使绘本故事更具视觉连续性
    - 播放：播放器，做成独立页面，方便从管理Tab按系列播放
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 我的：家长管控，家长可设置播放模式、管理模式，默认管理模式，管理切播放模式需要输入管理密码
    - 我的：配置修改，从Json文本升级为产品功能
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 启动：借鉴GSD，新增项目初始化引导 Skill`bootstraper`
    - 协同：借鉴GSD，新增多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 操作：引入 Skill`ship`，用于Ship环境测试
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；
