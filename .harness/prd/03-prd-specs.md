<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260405]
- 优化
    - 首页：FR，展示日期，未播放过的记录也展示日期，播放次数不展示(即没有/N)
        - 要求：MM.dd是名称前缀中的日期，不管有没有播放过
    - 制作：拍照，点返回按钮后、会卡顿在相机页面一会儿，希望是跳到制作页展示Loading、与`记录再次新制作`的风格保持一样
    - 制作：ISSUE，制作过程中切到了首页、在完成前切回了制作Tab，制作完成后不会跳转记录编辑页，制作过程中如果不切底部Tab、制作完成后能够正确跳转记录编辑页


---

[待办]
- 优化
    - 我的：阅读报告，在每周/每月之外，新增`半年`选项
    - 我的：阅读报告，新增`阅读统计`指标区，直方图展示听书本数(默认展示天级、可切换至周/月)，合适的图展示时段分布(每天按30分钟跨度切分成段，展示每一段的播放本书，如20:00-20:30)
    - 制作：制作任务支持并发，OCR识别只有要串行（受限于OCR API并发限制）
    - 首页：搜索，左侧结构化、支持选系列
    - 首页：缩略图，对齐图标、支持预先生成，生成时支持指定中心点、支持旋转图片方向
    - 首页：当前的布局，导致历史记录很难再次被发现
- 新增
    - 我的：阅读报告，每日/每周听读时长、听过多少本、最爱重听的绘本 Top3、连续听读天数；
    - 我的：时长管控，家长可设置单次/每日最长使用时长，到时间后温柔提醒"今天的故事时间结束了"并自动暂停
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 管理：分组，进化为可视化书架；
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 需求：多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 问题：新项目，缺少HE初始化引导，第一次任务完成时能自动回填 => 引入 Skill`bootstraper`，用于项目知识的初始化，兼容常用IDE
        - 引入 Skill`hr`，用于项目角色的初始化；
        - 用一个小任务，Trigger项目初始化；
    - 规则：引入 Skill`ship`，用于Ship环境测试
    - 规则：其它优化
        - 为coder和tester，为故事Story创建一个MVP、快速Run起来
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；
- 备选
    - 管理：FR，表头加一个Summay，展示绘本总数、系列数、播放数等；导出时隐藏
    - 首页：FR，卡片，支持定制缩略图；
    - 我的：配置修改，从Json文本升级为产品功能
    - 播放：图片生视频，使绘本故事更具视觉连续性
