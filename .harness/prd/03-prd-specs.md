<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260427]
- 优化
    - 管理：导出记录、传输记录时，清空playEvents、保留makeEvents，不影响`传输播放记录`模式
        - 迭代：管理右上角更多按钮，新增一个操作`导出(带统计)`，相比导出操作、导出(带统计)不清空playEvents；顺便，多个菜单新增一个`传输(带统计)`，传输记录时不清空playEvents
    - 管理：导出全部记录时，点击按钮后，要等很长时间、才弹出文件保存框；帮我看下阻塞原因，以及如何优化
        - 迭代：点完导出后，弹出专用的Loading页面、展示导出的准备进度，准备完成后再弹出保存框；保存完成后，直接调回记录列表页(不要停在Loading页)
        - 迭代：Loading 页闪的严重，因为 绘本名字程度不一、页面布局变化。状态描述文字，只需保留 进度M/N，无需展示当前正在准备的绘本名称
        - 迭代：修改导出记录的Loading页的风格，使和传输进度页保持一致

[20260501]
- 优化
    - 管理：Bug，发送方再次制作后，接收方删除对应老记录、传输过来新纪录(发送方再次制作)时会跳过
        - 迭代：再次制作的记录，被接收方判定为`完整性校验失败`
        - 迭代：接收方判定`完整性校验失败`，具体日志如下：
            - 跳过会话记录: 26.04.07 卡梅拉-我要找到朗朗, 文件夹: 26.04.07 卡梅拉-我要找到朗朗, 原因: 完整性校验失败: 完整性清单中的文件缺失: image_0.jpg
        - 结论：relativePath未考虑到iOS设备上/var是/private/var的符号链接
    - 管理：再次制作，要点文字又重复了一次，默认命名未删除`老命名中的日期`

---

[待办]
- 优化
    - 我的：记录分析，统计条叠状图，且通过长短体现数量差异
    - 我的：阅读报告，无法展示当天的统计
    - 管理：再次制作，未复用头像和封面图片，希望直接复用
    - 管理：Issue，导出状态页，准备完成时展示1/1、不正确
    - 管理：导出，改成先选目标目录、再直接写入目标位置
    - 管理：允许更新记录的照片，包括头像、封面、普通图片等
    - 我的：明确功能区，自上而下依次为播放、制作、其它
- 新增
    - 制作：要点图片，改进为要点视频
    - 重构：首页->播放，干掉制作Tab、制作页蜕化为无状态的二级子页面
- 备选
    - 管理：分组，进化为可视化书架；
    - 播放：图片生视频，使绘本故事更具视觉连续性
    - 播放：播放器，做成独立页面，方便从管理Tab按系列播放
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 我的：家长管控，家长可设置播放模式、管理模式，默认管理模式，管理切播放模式需要输入管理密码
    - 我的：配置修改，从Json文本升级为产品功能
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 启动：借鉴GSD，新增项目初始化引导 Skill`bootstraper`
    - 协同：借鉴GSD，新增多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 操作：引入 Skill`ship`，用于Ship环境测试
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；
