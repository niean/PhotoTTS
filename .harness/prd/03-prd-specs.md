<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260420]
- 优化
    - 启动：启动，在启动动画的同时，预加载首页卡片列表；现状是，首页卡片偶发先展示空、然后渲染出封面
    - 首页：[KF]卡片列表，从分页改为懒加载，懒加载长度复用现在的分页长度

[20260421]
- 优化
    - 首页：左上角增加排序方式切换按钮，样式和管理左上角按钮保持一致，支持列表、系列两种方式，默认列表方式、按日期+系列+名称排序，系列方式、按系列+日期+名称排序
        - 迭代：首页排序，除列表模式外、无需考虑播放计划对排序的影响

[20260425]
- 优化
    - 首页：播放列表，如开启播放计划、则不展示播放计划日期之后的记录
    - 管理：记录列表，记录行 在音频大小之后、展示播放次数，未播放则不展示该项
    - 管理：记录列表，从分页改为懒加载，懒加载使用现有的翻页阈值(类似首页的做法)
    - 管理：分组模式，新增`未读和已读`分组、切换顺序放到月份分组之后，未读在前、已读在后
    - 管理：顶导右侧更多按钮，拍照制作、选图制作间增加分割线(类似导入和导出之间)
    - 我的：[KF]播放计划展示本数和时长。播放计划Sector今日状态，如果未完成、给出播放绘本和时长预估(如3本 26分)，如果已完成、给出实际统计(如4本 31分钟)，播放时长预估和统计、简答加和绘本时长即可。播放设置按钮，如果开启了播放计划，将今日计划的数据分析、展示到`播放设置`按钮之后(如`今日计划6本 40分`)，字体要轻、避免喧宾夺主
- 治理
    - 修数：做一次性补数逻辑，给20260302及更早的未读绘本、补全history.json文件或部分信息。如果没有history.json文件，则新建；缺少makeEvents或playEvents信息，按如下内容补充

[20260426]
- 优化
    - 传输：[KF]传输记录支持部分成功，如中途失败了(如进展到65%)、已传输的记录要能保存成功
    - 记录：每条记录，增加一个完整性校验文件，对全部文件做md5校验。功能完成后，需要对存量记录、做一次性补数。这个功能，主要用于部分传输时的`完整记录`判断
        - 迭代：编辑记录、重新制作等操作后，要同步更新完整性校验文件
        - 迭代：传输记录、导出记录前，以记录文件现状为权威、检查&更新完整性校验文件，如遇不一致(更新)、打印日志告知`哪个记录的哪个文件校验信息被修正了`
        - Bug：发送新纪录、传输成功后，接收方导入0条记录且日志未打印原因
        - 迭代：传输播放记录后，接收方的整性校验文件也需要同步更新
        - 迭代：修改`完整性校验文件`的定位，它只应该存在于导出的记录、传输中的记录中，用作导入、接收的记录完整性校验；APP的磁盘记录中不应该有此文件、进而无需同步更新之
    - 记录：导入或接收记录，如果跳过、需要日志打印原因

---

[待办]
- 优化
    - 管理：允许更新记录的照片，包括头像、封面、普通图片等
- 新增
    - 制作：要点图片，改进为要点视频
    - 重构：首页->播放，干掉制作Tab、制作页蜕化为无状态的二级子页面
- 备选
    - 管理：表头加一个Summay，展示绘本总数、系列数、播放数等；导出时隐藏
    - 管理：分组，进化为可视化书架；
    - 播放：图片生视频，使绘本故事更具视觉连续性
    - 播放：播放器，做成独立页面，方便从管理Tab按系列播放
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 我的：家长管控，家长可设置播放模式、管理模式，默认管理模式，管理切播放模式需要输入管理密码
    - 我的：配置修改，从Json文本升级为产品功能
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 启动：借鉴GSD，新增项目初始化引导 Skill`bootstraper`
    - 协同：借鉴GSD，新增多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 操作：引入 Skill`ship`，用于Ship环境测试
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；
