
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI修改。

## 需求列表

[20260320]
- 新增
    - 记录：非首页embedded时，优化单条记录操作；单击记录 打开记录查看页，左滑记录 展示制作、导出、编辑、删除按钮（左滑的UI风格可参考微信消息）
        - 迭代：左滑展开的按钮样式调整，无需图标只要文字，形状为行登高的方块、四个按钮彼此相连；
        - 迭代：记录列表，非首页embedded时，单条记录行不再需要编辑、更多按钮，请帮去掉；
        - 迭代：记录列表，非首页embedded时，左滑展开的按钮，去掉导出；
        - 迭代：记录列表，非首页embedded时，单条记录行新增播放按钮，自此所有模式下的行按钮样式归一；
        - 迭代：记录列表，非首页embedded时，单条记录行恢复展示更多按钮；
        - 迭代：记录列表，删除确认时/后，记录未加载图标，取消删除后也不加载；
        - 迭代：记录列表，更多按钮返回时、需要隔1秒才能再看到更多按钮；
- 优化
    - 管理：新增底导Tab`管理`，将`我的-记录管理`迁移过来、作为`管理`Tab的首页，无顶导返回按钮、无左侧返回手势。具体的，
        - `管理`处于底导的第3个Tab，在`制作`之后、`消息`之前，离开Tab重新进入时页面状态重置；
        - `管理`Tab页顶导标题是"管理"；
        - 原有"我的-记录管理"NavigationLink推送路径，迁移到`管理`Tab页；
        - 迭代：Bug，记录的编辑、查看操作，无法打开页面；
        - 迭代：记录列表都有搜索框；`管理`Tab页搜索框默认不被顶导遮盖，只有首页embedded模式才被顶导遮盖；
    - 我的：底导干掉`消息`Tab，原`消息`Tab下的操作按钮分组，迁移到`我的`Tab页
    - 制作：制作页，样式归一，包括顶导间隙、页面布局等
    - 制作：制作完成（新制作或重新制作）后，跳转到`管理`Tab的记录编辑页，操作完成后停留在`管理`Tab页。具体的，
        - 记录：记录编辑页，提供取消、保存2个操作选项；新制作记录、存量记录，复用一个编辑页面；
        - 制作：制作完成、自动保存记录后，制作页状态自动清空；
        - 问题：记录编辑页，顶导缺少返回按钮，请参考记录查看页的顶导代码、使用统一的自定义组件；
        - 问题：Bug，制作完成后、自动切到了管理Tab，但并未打开编辑页；
    - 制作：图片少于5张时，跳过LLM绘本汇总环节、并打印日志
    - 记录：记录，音频时长信息不对，帮修复
        - 迭代：在代码中加入一次性任务，修复存量记录的音频时长信息
    - 播放：要点图片，支持用户自定义上传、区分横纵，要点图片=`系统内置+用户上传`
        - 入口：我的Tab - 要点图片，放到`设置`按钮上方；
        - 迭代：点击缩略图后、展示原图，点击原图后退出；
        - 迭代：点击缩略图后、展示的原图，其边缘、背景风格 跟播放器保持一致；
        - 迭代：点击缩略图后、加载原图，纵向动画的原图 加载动画方向要求从右到左；
        - 迭代：要点图片，添加图片支持多选，但只能加载成功第1张；
        - 迭代：要点图片，点击原图退出时不要展示动画；原图支持左右滑动，切到上一张、下一张原图；
        - 问题：要点图片，点击删除、弹出确认框的过程中，删除按钮会消失然后闪现；
        - 问题：要点图片，页面向上滚动时会遮盖顶导；要点图片，页面向上滚动时状态栏透明、能看到图片
        - 问题：要点图片，展示原图时未遮盖顶部状态栏、底部Tab；
        - 问题：要点图片，先点到的动画分类、无法正常加载大图，点击另一类正常，再回来点击时也能正常加载；

[20260321]
- 优化
    - 播放：要点图片，优化随机算法，使得横向/纵向连续播放N次、每张图片各出现1次。具体的，
        - 添加/删除图片后，重置；
        - App重启，不影响轮询；
        - 打印日志，显示播放使用的要点图片名称；
    - 制作：当前图片，从最后1张、改为第1张；

[20260322]
- 优化
    - 其它：iOS版本，最低版本从18.6降到18.1
    - 制作：制作页，拍照和选图后，当前图片使用第1张、而不是最后1张

[20260323]
- 优化
    - 记录：导入记录，readme中的记录列表按名称倒序排列
    - 制作：UI重构，展示制作过程的中间产物，包括OCR文本、LLM汇总等
        - 迭代：进度条和状态展示的位置是动态变化的，预期是固定在顶部的合适位置
- HE
    - 规则：修改流程，起始是harness知识库加载，然后是 superpowers`需求+头脑风暴 到 specs、plan、code`，最后是harness `结果验收、知识回填、任务总结`
    - 规则：流程简化，只保留功能迭代，其它交给LLM自决

[20260324]
- HE
    - 流程：代码实现，build要zero warnings，去掉git commit环节
    - 流程：需求探索和设计，改名为 需求探索？=> 维持
    - 流程：实现计划，是否改名为 计划制定？=> 改
    - 流程：任务总结，意图识别改名为需求描述？=> 改
    - 流程：需求确认，针对的是specs、而不是plans，计划制定需要[GATE]吗？=> 需要
    - 其它：当前的.harness/superpowers，是否依赖 cc安装superpowers插件？=> 不依赖
    - 其它：代码实现，如果输入指令没有明确指定，AI按照任务类型、自主决策，简单任务使用Inline Execution、复杂任务使用Subagent-Driven
        - ISSUE：修改harness skill后，GLM-5仍未生效、plan依然需要人工确认 => 直接修改superpowers.skill writing-plans.md => 已修改，AI按复杂度自动决策
        - NFR：修改.harness/superpowers/brainstorming.md，用户选择完方案之后，写入spec文档前、无需用户确认 => 已修改 brainstorming.md，移除 step 5 设计审批门禁，用户选方案后直接写 spec
    - 其它：superpowers写spec前、harness Phase 2 spec落盘后，都会触发AskUserQuestion，重复了。怎么修正流程，才能避免2次确认？=> 已修改 brainstorming.md
    - NFR：分析harness对superpowers的定制化修改，记录到README文件，用于后续Follow superpowers版本升级
- 新增
    - 播放：当前只能即时播放1个记录，希望能有计划的连续播放
        - 迭代：两次播放之间暂停3秒，增加一个平滑的Loading页面；
        - 迭代：连播列表，默认是发生在同一日期的所有记录，跨天则停止连播；
        - 迭代：过渡页面，文字展示默认横向(而不是当前的纵向)；
        - 迭代：过渡页面，文字加上 播放进度，如 `2/5：记录名称`；
        - 问题：Bug，点击播放下一个后，首页是上一个记录的图片、第二页恢复正确；
        - 迭代：播放器的 播完本集按钮，默认关闭，因为连播时状态不对；
        - 问题：Bug，播完本集未生效、依然连播了；
        - 迭代：播放器的 播完本集按钮，改名为`同日连播`，默认打开、支持连播，关闭后不支持连播；含义发生变化，代码名称也需要修改
- 优化
    - 首页：首页和管理的UI重合度高，我希望修改首页样式，突出其播放职能。具体的，
        - 脑暴：首页改为双列卡片网格布局；保留搜索和分页；
        - 调整：搜索框默认被顶导遮盖，下滑时出现，切换Tab再切回恢复遮盖；iPad，卡片展示，每行4个；
        - 调整：拍照/选图制作入口，从首页、移动到管理Tab上方(类似首页)；
        - 迭代：首页卡片布局不符合预期，长宽不一、相互覆盖，希望①限定卡片长宽，②使用缩略图；
        - 迭代：首页卡片，记录名称太长展示不全，去掉前缀`yy.MM.dd `、字体变细一点；
        - 迭代：首页，功能区底色设置为白色、跟管理Tab的底色保持一致，顶导底部Tab维持原状；
        - 迭代：首页卡片，支持左右滑动手势，左滑向前翻页、右滑向后翻页；
        - 问题：首页，卡片数量不足以触发滚动条时，搜索框无法可见；
        - 迭代：首页、管理Tab，翻页按钮风格保持一致 => 抽象自定义翻页组件，制定默认高度；
    - 记录：首页、管理Tab，搜索框增加确认按钮，点确认触发搜索、不自动触发搜索；
    - 播放：播放器，在播放下一集的按钮后方、展示播放进度，形如` (2/5)`；
        - 迭代：播放进度文本，字体过大、过粗太丑了；
    - 播放：连播时，播放最后一集后、播放器未正确退出；这是一个很严重的问题，请慎重对待；
    - 播放：ISSUE，播放器，拖动进度条、会被图片翻页手势劫持，要求进度条手势不要被图片抢走；
        - 追加：进度条的拖动手势，容易被页面单击影响；点击进度条，希望播放进度同步跳转到对应的位置；
    - 播放：FR 播放器进度条，播放状态时也允许拖拽，现状是暂停时才能拖拽；

[20260325]
- 优化
    - 首页：iPad上每行卡片数调整为3个；
        - 迭代：首页，列表每页记录数 调整为每行卡片数的10倍，iPad 30、iPhone 20；管理Tab亦是如此
    - 制作：ISSUE，OCR制作进度，超过50%了、不正确；
    - 启动：启动页，预加载首页Tab列表，使得用户打开首页时、快速看到列表（放弃任务，首页每次都重新加载数据）
- 新增
    - 列表：支持通过左右滑动手势翻页，左滑显示下一页，手势识别区为列表全域；首页、管理Tab的列表均如此（手势存在冲突，放弃需求）
    - 记录：记录从1台设备、传到另1台设备，目前采用文件导出、AirDrop传输、再导入的方式，路径很长；我希望，2台设备APP间直接传递数据，请帮我实现
        - 迭代：E2E传输时，选择设备页面布局有问题、看不到设备列表；
        - 迭代：E2E传输1条记录，打包环节内存飙升到1.5G、导致APP被OS杀死；接收方，接收数据时是否也有内存飙升问题？请检查、修复
        - 迭代：E2E传输时，接收方只有点击某个Tab时、才会跳出接收弹窗，希望能自动弹出；
        - 迭代：E2E传输时，发送方只选了1条记录，接收方显示56条(全部)；
        - 迭代：E2E传输时，发送方页面使用标准的CustomZStack页面，不要sheet(顶部有Gap)；
        - 迭代：E2E传输时，接收方失败，错误信息 `导入失败: 请选择导出包目录(含 export_manifest.json) 或单个会话目录(含 record.json)`
        - 迭代：E2E传输时，发送方进度条显示`正在接收`，正确的是`正在发送`；
        - 迭代：E2E传输时，接收方处在首页Tab时，成功接收记录后、首页列表没有自动刷新；管理Tab，估计也有类似问题；
        - 迭代：E2E传输时，发送方发送完成后，`设备传输`页面 直接退出、不要再自动刷新设备列表了；
        - 迭代：E2E传输时，发送方成功接收1次后，设备无法再被发现、从而无法再次传输；

[20260326]
- HE
    - 问题：Skill迭代功能 的P3进入P4，偶尔还是需要用户确认；帮评估下，直接修改./skills/superpowers/下的skill，干掉被遮盖的用户确认、干掉gitcommit
    - 技能：参考`Skill迭代功能`，给Harness新增`Skill Bug修复`，参考流程是：Phase 1: 任务调度 => ## Phase 2: BugFix(使用./superpowers/的 Bugfix技能) => ## Phase 3: 结果验收 => ## Phase 4: 知识回填 => ## Phase 5: 任务总结，你可以根据实际情况、帮我优化流程
    - 问题：plans、specs的active单例，导致任务无法并行，请放开plans、specs的单例限制；
    - 问题：superpowers阶段命名为`Phase N`的、均改为`P N`，避免和Harness冲突
- 优化
    - 管理：FR，去掉制作入口
    - 我的：FR，实时监控，打开app后即启动，而不是打开监控页面再启动
    - 播放：Bug，连播列表，期望是使用记录列表展示名称的前缀中的日期，当前并非如此
    - 播放：FR，支持倍速，选项包括x1 x1.25 x1.5 x2，需要处理好 音频/图片的同步；
        - 入口，播放设置面板
        - 时机，即时生效
        - 持久化，持久化记忆
        - 迭代：播放，倍速增加x0.9选项
        - 迭代：播放，倍速增加x1.1选项、去掉x2选项
    - 记录：E2E传输时，接收方如果正在播放，则暂停播放、但不要退出播放全屏页面；
        - Bug依然存在。你看下是不是接收确认框、状态展示页面的影响？
        - Bug依然存在。日志`PlayView: 收到传输邀请，暂停播放`，但播放器退出了、不符合预期；
        - 新Bug：接收确认时，播放器没有退出全屏，同意接收后、接收状态不可见，接收完成后、通知框又将播放器退出全屏了；
    - 记录：E2E传输时，握手阶段(传输数据前)应先确认记录IDs、去重避免无效传输；
        - Bug：E2E传输时，重复记录并未提前去除、而是进行了无效传输
        - 迭代：E2E传输时，记录IDs去重应该发生在 接收通知前，而不是之后
        - 迭代：E2E传输时，接收方记录IDs去重后，发送方也应该展示下去重的情况
    - 记录：E2E传输时，传输接收相关的页面抽象为公共组件，方便在多个入口调起，公共组件至少包括接收通知、进展状态、完成通知
    - 记录：E2E传输时，接收方除了显示进度、还要显示第N条记录，接收、发送状态展示对齐；

[20260326]
- 优化
    - 播放：FR，播放器，按钮布局调整，
        - `更多`按钮，放到右下角，改名为`设置`、改用设置图标，位置风格布局跟`播放`按钮保持一致；点击设置按钮，弹出播放设置面板，紧贴上边缘、边缘，支持上下滚动；
        - `关闭`按钮移动到右上角；
    - 播放：FR，播放器设置面板倍速选项支持多行，倍速选项调整为x1 x1.1 x1.25 x1.5 x2 x3，每行最多3个选项按钮、支持多行
- HE
    - 治理：superpowers，任务名为`Task N`的、均改为`T N`，步骤名为`Step N`的、均改为`S N`，避免和Harness冲突；同步更新 superpowers/README


---

[待办]
- 优化
    - 治理：按照Anthropic的最佳实践，检查Harness知识库的RULE、SKILL、Prompt等描述；
- 新增
    - 管理：支持按绘本系列分类
- 备选
    - 我的：配置修改，从Json文本升级为产品功能
    - 播放：图片生视频，使绘本故事更具视觉连续性
- HE
    - 问题：新项目，缺少HE初始化引导，第一次任务完成时能自动回填 => 引入 Skill`bootstraper`，用于项目知识的初始化
    - 规则：引入 规则`LESSONS.md`，记录错误 => 升级为规则（可能高度LLM个性化）
        - 引入 Skill`hr`，用于项目角色的初始化；
        - 用一个小任务，Trigger项目初始化；
    - 规则：引入 Skill`ship`，用于Ship环境测试
    - 规则：引入 Agent`troubleshooter`，专门用于排障
    - 规则：其它优化
        - 为coder和tester，为故事Story创建一个MVP、快速Run起来
    - 规则：引入 工具`worktree`，支持多任务并行开发
    - 规则：简单迭代，如修小Bug、改小特征，缩短完成耗时，没必要加载全局知识库、负载代码扫描等
    - 重申：请严格按照 `迭代步骤`执行，禁止擅自修改跳过，特别是`意图确认`、`结果验收`、`新技术债修复`(如果有)
    - 问题：ISSUE，既有.harness项目升级麻烦，考虑将AGENTS.md的内容、放到.harness/HARNESS.md，AGENTS、CLAUDE保留引用 @.harness/HARNESS.md
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；