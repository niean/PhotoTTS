<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260613]
- 优化
    - 制作：要点视频，未体现在播放的进度条上
- 新增
    - 管理：[KF]编辑会话，允许更新照片。每张图片，被点击后跳出更新页面，允许旋转、重置
、上传图片，点击保存后替换原图片，OCR文字、LLM要点、TTS均无需更新。如果第一张图片被更新，头像、封面则恢复到初始默认状态（等待用户编辑）

---

[待办]
- 优化
    - 管理：Issue，导出状态页，准备完成时展示1/1、不正确
- 新增
    - 运营：内容分发，记录250+后，单机存储空间不足，引入百度网盘等做分发
- 备选
    - 管理：再次制作，未复用头像和封面图片，希望直接复用
    - 管理：分组，进化为可视化书架
    - 管理：导出，改成先选目标目录、再直接写入目标位置
    - 播放：图片生视频，使绘本故事更具视觉连续性
    - 播放：播放器，做成独立页面，方便从管理Tab按系列播放
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 制作：并发时，在制作页增加入口、方便制作下一个
    - 制作：[疑难杂症]Bug，并发时，制作完成的记录、还在展示状态`制作中`，切Tab后会刷新为正常状态；期望制作完成的记录不展示任何制作状态、而是展示正常记录信息(图片张数、音频时长、记录大小等)
    - 我的：家长管控，家长可设置播放模式、管理模式，默认管理模式，管理切播放模式需要输入管理密码
    - 我的：配置修改，从Json文本升级为产品功能
    - 重构：首页->播放，干掉制作Tab、制作页蜕化为无状态的二级子页面
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 启动：借鉴GSD，新增项目初始化引导 Skill`bootstraper`
    - 协同：借鉴GSD，新增多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 操作：引入 Skill`ship`，用于Ship环境测试
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；

---

## 待真机验收
- 播放：我的-播放设置-播放计划，默认不开启
- 播放：卡片状态更新不及时，包括播放计划(已验证)、次数
- 制作：OCR文字的6个点号替换为3个，防止TTS间隔过长
- 管理：传输记录，先让接收方确认剩余存储空间是否足够，避免传完后才失败
- 管理：记录制作中记录，在管理页List中实时展示状态
