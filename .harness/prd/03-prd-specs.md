<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260413]
- 优化
    - 首页：播放计划，未完成记录仅限最近30天，超出30天后不参与播放计划之计算
    - 首页：当前的布局，导致历史记录很难再次被发现（结论：暂不优化，通过系列筛选来缓解）
    - 我的：设置，改名为`系统设置`，同步更新代码相关命名
    - 我的：新增播放设置，放到要点图片下、系统设置上；播放设置页，增加播放计划区、允许重置
    - 我的：播放设置，新增开关控制、默认开启，关闭后不再展示播放计划
    - 我的：调整按钮布局，Section1依次放置阅读报告、播放设置、播放历史，Section2依次放置要点图片、制作设置(原设置)、制作历史，Section3维持现状(依次为调试日志、实时监控、更新记录、关于)
    - 首页：同日连播，名称和含义都修改为`计划内连播`，今天播放计划内的记录连播、其它不连播
    - 制作：要点图片，内置图片从固定数量、改为动态获取，更新对应的播放队列逻辑等相关代码
    - 制作：要点图片，播放队列可视化、方便测试，要点图片的播放队列、放到`我的-要点图片-X向动画-添加图片按钮`的下方
        - 迭代：已播放过的要点图片，按照播放编号、放到尾部。新增已播队列，记录已经播放的状态
        - 迭代：播放队列，新增重置按钮，UI样式跟`新增图片`保持一致，图标选择合适的
        - 迭代：重置的状态提示，动画自下而上、非常不协调，把状态提示全部干掉吧；另外，重置后，播放队列未更新为 最新顺序
        - 精调：我的-要点图片-播放队列，`播放队列`字体不协调，修改为 EndPictSectionView.subtitle的样式

[20260414]
- 优化
    - 文档：产品流程调整，制作->保存->播放，解耦记录的生产和使用。更新项目说明文档，按需更新PRD文档
    - 制作：制作页，页面和按钮样式优化
        - 缩略图所在行的按钮，删除播放按钮，对应的代码、如不再使用也需要删除
        - OCR按钮，从缩略图行、移动到顶导更多菜单，命名`OCR`
        - TTS，失败提示的顶部、跟状态展示对齐(如`OCR识别`)
        - 顶导更多菜单，新增LLM、TTS按钮，支持断点继续
    - 制作：[KF]制作允许断点继续。当文字内容(OCR结果)存在时，LLM按钮可点击、点击后触发后续的完整制作流程(LLM TTS直至保存)；当文字内容(OCR结果)存在时，TTS按钮可点击、点击后触发后续的完整制作流程(直至保存)
        - 操作：制作过程中，右上角展示取消按钮(圆圈叉号)，点击后清除数据、删除临时记录、退出制作；制作失败后，右上角展示顶导的更多按钮(不要修改菜单项)，不主动清空数据状态或删除临时记录
        - 问题：TTS失败时，已经保存的临时记录(草稿记录)中没有展示文本内容(应该展示)
        - 问题：TTS失败时，每次重试都会新建一个临时记录，期望是重复已有的一个临时记录

[20260415]
- 优化
    - 制作：制作页允许加载完整记录。记录点击制作按钮、跳转制作页后，不要触发制作动作、只完成数据内容加载，交由用户自己决定从哪个环节开始(更多菜单中的OCR LLM TTS)
        - 状态：复用记录的状态数据，还原整个状态详情；进度条说明是`再次制作`还是`制作中`，并给出实际完成的进度
        - 补充：状态详情，之前缺少TTS的统计信息；请参照OCR、LLM的信息，实现TTS的状态展示
        - 迭代：`整体100%`的状态下，系制作完成状态、不展示右上角的取消按钮 => 问题：不展示取消按钮后，更多按钮没有移动到最右侧
        - 迭代：`整体100%`的状态下，直接保存记录后、未自动生成故事名；保存后的记录，播放时没有正确展示要点图片 => 问题：加载记录`整体100%`的状态下，保存记录后跳转到了老页面，应复用编辑页(跟正常保存对齐)
        - 迭代：加载完整记录、再次保存为新纪录时，未正确复用老记录的头像和封面
    - 制作：制作过程中，不展示更多按钮
    - 传输：新增模式，只传输history.json文件、不传输记录数据，产品交互跟记录传输保持一致
        - 修复：传输播放记录的菜单按钮，只应该出现在`选择记录列表`后的更多按钮中
        - 修复：传输播放记录时，记录重复(说明存在)是合理的，发送方应该传输过来&接收方应覆盖本地记录，当前的做法是发送方传输跳过、或接收方未覆盖本地记录

[20260417]
- 优化
    - 制作：[KF]支持后台并发制作，加载到制作页前台、通过管理页的记录操作按钮，OCR识别要求多任务间串行（受限于OCR API并发限制）
        - 问题：后台记录，没有加载到前台的入口；

[20260418]
- 新增
    - 制作：[KF]支持音频分段合成，防止字数太多调用API失败。具体的，
        - 音频分段制作后，播放时要合并在一起播放，并且做到音频、图片同步换页(跟现状效果保持一致)
        - 音频分段时，每张图片的文字要放到一段音频内，音频分段的字数阈值默认2000(定义为常量)
        - 迭代：TTS音频分段时，对分段做1-N的编号，多个分段并发执行、并发度5(定义为常量)；日志打印每段音频制作对应的图片编号、耗时等信息，制作进度、状态也展示音频分段进展情况
        - 迭代：音频分段时，补全导入导出传输的处理逻辑
        - 迭代：会话详情、编辑页，展示音频段数信息
- 优化
    - 制作：并发制作时，制作中的后台记录加载到前台、没有操作入口
    - 制作：再次制作时，点击TTS后，制作状态页无法展示OCR、LLM结果，记录信息中也没有OCR、LLM的耗时等信息；点击LLM后，制作状态页也不展示OCR结果
    - 播放：Bug，音频分段后，拖动进度条、出现同时播放两段音频的情况

[20260419]
- 优化
    - 管理：传输记录，传输一次后，再次传输时看不到对方设备、或点击传输后没响应
    - 管理：传输播放记录，返回记录列表页时、还是选择页面，期望是常规的记录列表页(跟传输记录后的返回页、保持一致)

---

[待办]
- 优化
    - 管理：传输，接收方已存在的记录、发送方自动不传输，避免浪费带宽和时间；做好双方的弹框提示
    - 制作：记录重新制作，TTS音频分段制作，完成后、合并&保存为一条完整的音频了
    - 启动：启动，去掉模拟的启动行为，改为预加载首页卡片列表
- 新增
    - 管理：记录，标注适龄信息，如3-6岁
    - 制作：要点图片，改进为要点视频
- 备选
    - 管理：允许替换特定照片，含头像、封面
    - 管理：表头加一个Summay，展示绘本总数、系列数、播放数等；导出时隐藏
    - 管理：分组，进化为可视化书架；
    - 播放：图片生视频，使绘本故事更具视觉连续性
    - 播放：播放器，做成独立页面，方便从管理Tab按系列播放
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 我的：配置修改，从Json文本升级为产品功能
    - 我的：时长管控，家长可设置单次/每日最长使用时长，到时间后温柔提醒"今天的故事时间结束了"并自动暂停
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 需求：多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 问题：新项目，缺少HE初始化引导，第一次任务完成时能自动回填 => 引入 Skill`bootstraper`，用于项目知识的初始化，兼容常用IDE
        - 引入 Skill`hr`，用于项目角色的初始化；
        - 用一个小任务，Trigger项目初始化；
    - 规则：引入 Skill`ship`，用于Ship环境测试
    - 规则：其它优化
        - 为coder和tester，为故事Story创建一个MVP、快速Run起来
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；
