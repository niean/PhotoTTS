<!-- SUMMARY: 产品功能规格的迭代演进记录，按版本记录各功能模块的详细需求变更 -->
# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI阅读或修改。

## 需求列表

[20260328]
- HE
    - 问题：上下文过大时，需要Harness怎么处理？按照Anthropic最佳实践，有何推荐解法
- 新增
- 优化
    - 传输：FR，从传输状态页、返回列表页后，列表页取消选中状态；
    - 传输：ISSUE，发送方第2次传输，接收方不弹出接收框
        - 原因：MCSession.disconnect()后需等待底层状态清理再创建新session；修复：reset后延迟0.5秒再startAdvertising；
    - 制作：FR，LLM分析，名称和汇总偶尔带`1.`或`2.`开头，筛除去掉；
    - 制作：FR，制作过程中不要熄屏；
    - 制作：FR，LLM分析的提示词加上常见系列，提升名称准确率；
    - 制作：FR，拍照，右下角按钮从切换摄像头、改为显示上1张图片的缩略图；
        - 迭代：缩略图使用圆形图标，跟返回按钮保持一致；未拍摄时，缩略图也应使用圆形图标、白底色(现状是完全看不到缩略图标)；
    - 制作：FR，处理统计，缺少LLM耗时，补充并放到OCR耗时之下、TTS耗时之上(如果有LLM环节)；
        - 迭代：ISSUE，记录查看，处理统计中缺失LLM耗时；
        - Bug：会话详情-处理统计，LLM耗时未正确展示；导出记录record.json中llmDuration是对的(13s)，但`会话详情-处理统计`未展示

[20260329]
- HE
    - 规则：引入lessons/知识，记录通用/项目两级的教训。定位 ①AI自主维护（人最多给点提示、建议），②教训可定期升级为规则
        - 通用教训，紧与Harness相关，与具体的语言、框架、项目无关。据此，更新现状
    - 规则：新增Skill迭代Harness文档，地位类似迭代功能
- 优化
    - 播放：Bug，全屏播放时，iPhone、iPad均会自动熄屏；这个Bug实在代码版本ae21bef931de2a0c62f1e096c66dfecf209f3846之后引入的
    - 息屏：防息屏，覆盖页面至少包括 播放器(全屏播放)、传输记录、制作过程；请按照lessons教训，检查下是否需要修改
    - 制作：FR，制作过程展示更多细化，①百分比变化后、同步展示状态细节，②展示OCR+LLM阶段的字数、耗时等信息
        - 迭代：OCR，批次未正确展示；LLM，耗时未展示；
        - 迭代：OCR，批次展示在明细区、而不是状态说明中；
        - 迭代：OCR，第1批次结束后，OCR识别的文字结果、需要展示在过程明细区域；
        - 迭代：制作过程中的`绘本分析`=>`LLM绘本分析`；

[20260330]
- HE
    - Agent：独立Reviewer，允许指定model；
    - Agent：独立Coder，并行时允许指定model；
    - Agent：定义Designer、Planner，暂时复用主Agent；
- 优化
    - 制作：FR，制作过程页面，右上角的停止按钮(圆圈叉号)、顶导右上角按钮，两个按钮挨的很近、容易点错；期望，制作过程独立为页面、重置顶导，底色维持制作空白页(维持原有透明度)
    - 制作：FR，记录再制作，跳转到制作页面后、会持续2-4秒的空白页、然后才展示出制作过程；期望跳转到制作页面后即刻展示制作过程页面、提示加载中
    - 管理：修复管理页删除记录闪变，记录删除某一项时，列表会先少一项、再加回来，闪变一下；
    - 管理：分组模式下，修改记录名称后、UI列表中的名称未刷新，期望能刷新；
    - 管理：分组模式下，优化手风琴的UI样式（放弃执行，AI改进的样式更差）；
    - 管理：分组模式下，单击分组号(行最左侧)没反应，是因为左滑手势冲突、还是UI组件特征；期望，单击有响应；
    - 我的：名称，过滤掉无效前缀`Clone 1 of`；
    - 管理：FR，记录，单条记录不再展示更多按钮，但保留单条导出、传输的功能代码；

[20260331]
- HE
    - Agent：Reviewer，spec改为可选、兼容fix-bug
    - 模板：优化Skill提取Harness模板
- 优化
    - 汇总：要点图片，支持更多张；

[20260401]
- HE
    - 架构：重新定义分层架构，明确名词含义Workflow、Agent、Skill分工（干掉角色概念）；
- 优化
    - 管理：FR，系列/月份维度下，数据容器未铺满全屏、下半部分漏出底色，数据容器应该铺满全屏；
        - 迭代：Bug，数据区底色对了，顶导底色和其它页面一致，但底导底色不一致了；
        - 迭代：Bug更甚，顶导也被设置底色、跟其它页面不一致了；
        - 迭代：顶导、底导都要保持透明，不被设置底色；
        - 问题：遗留问题，底导白底色不符合预期；
    - 首页：卡片，展示最后一次播放日期和播放总次数，标题上方(跟标题左对齐)、小白字，内容形如 `04.01(2)`（04.01是最后播放日期、2次是总播放次数）；如未播放过，则不展示
        - 迭代：播放统计，放到图片的左下角、跟标题左对齐
        - 迭代：播放统计，内容格式改为 `04.01/2`

[20260402]
- HE
    - 架构：HE，拆分通用FW和项目KB；
    - 文档：HE，将Lesson提取为规则；
    - 治理：扫描，补全依赖方向、区分等级；
- 优化
    - 首页：FR，先参考`首页的搜索框默认被顶导遮盖，点击Tab一次后下拉展示出来`，然后加入 `点击Tab后List切到第1页`的逻辑
        - 迭代：首页same-tab tab后，List确实切到第1页了；但是，List滚动到top的行为也被拦截了，同时如果List当前就在第1页的话、页面在无需切页的情况下闪动一下
    - 管理：平铺模式下，支持same-tab tab后List切到第1页了，同时不破坏原有动作；如果List当前就在第1页，则无需动作。实现方案，可参考首页

[20260403]
- 新增
    - 我的：实现页面`阅读报告`，放到`我的-播放历史`按钮上方(同组)。功能规格参见 spec-260402-reading-report.md（参考洪恩绘本的功能）

[20260404]
- 优化
    - 管理：FR，系列分组，List未铺满数据区时、与底部Tab有GAP；参考日期分组的做好，消除GAP
    - 我的：阅读报告，新增`最近收听`列表区，放到`最爱重听`下方，根据每周/每月、按时间倒序展示所有播放记录B，行尾展示播放日期
        - 继续迭代：最近收听列表分页，分页数量复用记录列表
        - Bug：列表翻到第2页后，屏幕默认是空的，点击一下后、阅读报告顶部统计展示出来
- HE
    - 产品：更新prd-baseline，在`页面结构`之下、新增`页面风格`，比如颜色、白底、字体、圆角等，根据项目现状、完成初始化

[20260405]
- 优化
    - 首页：FR，展示日期，未播放过的记录也展示日期，播放次数不展示(即没有/N)
        - 要求：MM.dd是名称前缀中的日期，不管有没有播放过
    - 制作：拍照，点返回按钮后、会卡顿在相机页面一会儿，希望是跳到制作页展示Loading、与`记录再次新制作`的风格保持一样
    - 制作：ISSUE，制作过程中切到了首页、在完成前切回了制作Tab，制作完成后不会跳转记录编辑页，制作过程中如果不切底部Tab、制作完成后能够正确跳转记录编辑页


---

[待办]
- 优化
    - 我的：阅读报告，在每周/每月之外，新增`半年`选项
    - 我的：阅读报告，新增`阅读统计`指标区，直方图展示听书本数(默认展示天级、可切换至周/月)，合适的图展示时段分布(每天按30分钟跨度切分成段，展示每一段的播放本书，如20:00-20:30)
    - 制作：制作任务支持并发，OCR识别只有要串行（受限于OCR API并发限制）
    - 首页：缩略图，对齐图标、支持预先生成，生成时支持指定中心点、支持旋转图片方向
    - 首页：当前的布局，导致历史记录很难再次被发现
- 新增
    - 我的：阅读报告，每日/每周听读时长、听过多少本、最爱重听的绘本 Top3、连续听读天数；
    - 我的：时长管控，家长可设置单次/每日最长使用时长，到时间后温柔提醒"今天的故事时间结束了"并自动暂停
    - 制作：TTS支持背景音乐；识别对话和旁白，用不同 TTS 声音分角色朗读；
    - 管理：分组，进化为可视化书架；
    - 重构：借鉴洪恩，播放页改为横屏布局
- HE
    - 需求：多模块协同管理。Harness级联/REF，项目A新增功能、需要调用下游项目B，项目A/B均有Harness，试问A是否能查阅B的Harness、从而精准Coding？
    - 问题：新项目，缺少HE初始化引导，第一次任务完成时能自动回填 => 引入 Skill`bootstraper`，用于项目知识的初始化，兼容常用IDE
        - 引入 Skill`hr`，用于项目角色的初始化；
        - 用一个小任务，Trigger项目初始化；
    - 规则：引入 Skill`ship`，用于Ship环境测试
    - 规则：其它优化
        - 为coder和tester，为故事Story创建一个MVP、快速Run起来
    - 其它
        - 代码实现，2+个LLM交叉验证；
        - 需求探讨，花费大部分的项目时间；
        - 业务验收，是需求的一部分，由人主R；
- 备选
    - 管理：FR，表头加一个Summay，展示绘本总数、系列数、播放数等；导出时隐藏
    - 首页：FR，卡片，支持定制缩略图；
    - 我的：配置修改，从Json文本升级为产品功能
    - 播放：图片生视频，使绘本故事更具视觉连续性
