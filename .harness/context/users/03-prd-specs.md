# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI修改。

## 需求列表
[260304]
- 优化
    - 消息：调试日志，简化内容、提升交互
        - 简化内容，干掉Apple系统日志的原始结构化输出片段 `OSLOG-AA9F2DF2-B46D-4921-94CC-291C9ED07AF9 7 80 L 1a {t:1772585718.989001,tid:0x28ab75}`
        - 简化内容，修改时间头，如从`[2026-02-19 16:24:56.046]` 改为`0219T16:24:56`
        - 提升交互，调试日志改为时间倒序，最新的日志放到最前面
        - 提升交互，支持选中特定文字、复制到剪切板；仅选中所有文本不满足需求，需要支持选中特定文本
    - 我的：APP支持简单的身份名称，如设备名称iPhone，允许自定义修改；要保持APP的工具属性
    - 消息：制作、播放历史，在保证UI功能不变的情况下，存储方式从单独存储、改为合并到会话记录(从而可以随图片&音频一起导入导出)，单次的制作、播放记录增加发起者的身份信息
        - 追加：一次性执行，给每个会话记录补全制作历史，制作时间使用会话的更新时间、使用当前身份
        - 追加：一次性执行，将现有的播放历史，补充到会话记录中
        - 追加：一次性执行，删除单独存放的制作、播放历史信息
        - 追加：清理一次性执行类的任务
        - 追加：getAllSessionMetadata添加caller，识别调用方
    - 播放：播放页面，图片左右滑动后，音频应该调整到当前图片对应的位置
    - 播放：增加防御，任意时刻只允许一个记录播放，如有其它记录在播放、不允许触发新的播放
- 新增
    - 播放：播放时，支持双击播放页面、切换暂停/播放状态
        - 追加：大图左右滑动，播放时禁止、暂停时允许；增加简单的动画效果，使左右滑动更平滑
    - 播放：播放时，支持通过Siri切换暂停/继续，支持通过Siri控制音量
        - 追加：播放时，支持通过Siri 退出播放，语音指令是`退出播放`（结果未达成，系Siri系统限制）
        - 问题：Siri控制暂停/继续播放时，会弹出"正在播放"进度条、影响体验，帮禁用之（结果未达成，系Siri系统限制）
    - 制作：制作时，允许制作转为后台操作，从而使APP能做其它事情(如播放已经存在的会话记录)。
        - 这就需要一个制作记录，方便用户找回后台制作，进而查看制作结果、保存记录等；
        - 制作记录，希望能够复用会话记录，既能简化产品功能，还能拍照后生成记录、防止图片遗失；
        - 制作复用会话记录，制作过程中要 补全分类(如制作中)、补全元信息(如记录名称`YY.MM.DD 未命名`)，制作结束后也要同步更新元数据，最终的保存操作可能仅仅是改一下记录名称
        - 其它说明：只允许1个后台制作任务；不允许修改现有制作页面的UI，处理状态展示也尽量复用制作页
        - 追加：后台制作，制作完成后、不要自动播放，防止重装当前的操作；普通制作维持自动播放

[260305]
- 优化
    - 执行Skill治理巡检时，因上下文过大、AGENT舍弃了`AGENTS.md校正`子任务。通过提示词，要求AGENT自动压缩上下文，确保任务被完整执行。其它Skill，会有类似问题吗？怎样预防自动化进程被中断（已补齐多步任务上下文管理）
    - 执行Skill提取-Harness模板时，子代理结果被截断了，Agent需要读取完整文件内容。请修改Skill描述，避免该问题
    - 多Agent：当前项目是单个主Agent，我希望：不同功能分别由专属Agent完成。如，整体调度、理解意图、实现代码、测试验收。请设计下调度架构（限制：Cline只允许主Agent修改文件）
        - 追加：每次任务，我需要明确的知道 ①使用了多Agent架构 ②每一步是哪个Agent执行的
        - 追加：多Agent是新引入的功能，之前是单Agent架构。检查下 Harness知识库，确保没有重复性的描述（特殊强调可例外）
- 新增
    - 记录：记录列表页，支持按照名称模糊匹配、搜索记录，搜索框类似微信、放到记录列表的上头
        - 追加：我希望搜索框跟记录一起滚动，底色也和记录保持一致，高度定制；当前搜索框固定在顶导下方、不会滚动
        - 追加：搜索框功能满足了，但UI太丑了。希望搜索框 默认情况下被上头遮住，只有记录下拉一个搜索框行高时、才能看到
    - 记录：记录列表页，分页展示、每页最多20个记录，页面底部支持分页切换，数据不要全量加载、而是按需；搜索框不计入分页，且始终在头部

[260306]
- 优化
    - Harness：单Agent切到多Agent模式后，Harness文档中的规范、还有哪些需要修改？请全面检查
    - Harness：Harness文档，正确的引用结构应该是怎样的？比如AGENTS.md是顶层
        - 追加：根据上述梳理，制定文档引用规则、枚举特例，记录到合适的文档，比如 `治理技能`新增 `引用关系治理`
    - Harness：Subagent，改名为Subskill(本质是Task)
    - Harness：文档压缩，维持 AI 可理解性和信息量不丢失的前提下，提炼、简化、压缩文档内容，消除冗余文字，节省上下文
        - 反馈补偿：本次压缩过程，舍弃的特殊标注事项，打印在消息框、等人工确认是否找回
    - Harness：除功能迭代外，再抽象`修改代码`的任务类型。修改代码，任务流程和`功能迭代`类任务保持一致，也要使用Skill`功能迭代`
        - 追加：新 Task 或同一 Task 内的第 2+ 次反馈，都要在消息框说明任务类型，和`本次任务采用X Agent 架构`放在一起
    - 制作：CustomCameraViewController，干掉选图功能、清理相关代码，相机页的选图按钮、改为最新1张图片的预览。不要擅自修改其它样式或行为
        - 追加：原选图按钮，从最新1张图片的预览、改为前后相机切换(按钮和标准相机保持一致)
    - 播放：暂停播放后滑动图片，再次播放时，音频应该从当前图片对应的位置开始
- 新增
    - 制作：OCR，OCR模型除默认选择的豆包之外，还要支持OpenAI Compatible类型，支持通过配置文件、切换OCR模型。我已经给出了配置文件样例 Resources/config_local.json
        - 追加：调试需求，OCR日志打印Provider(如豆包、OpenAI)
    - 制作：TTS，TTS除默认选择的火山TTS之外，还要支持阿里千问3-TTS-Flash，支持通过配置文件、切换TTS供应商。我已经给出了配置文件样例 Resources/config_local.json。
        - 资料：阿里千问3-TTS-Flash的文档，可参考 https://help.aliyun.com/zh/model-studio/qwen-tts-api?spm=a2c4g.11186623.0.0.2dd36cc58btaAo#b1320a1664b9a
        - 追加：阿里千问3-TTS-Flash的调试，请你先写一个功能测试，使用Resources/config_local.json中的配置、进行真实调试，测试通过后再回填到正式代码
        - 追加：阿里千问3-TTS-Flash的音频格式wav，火山TTS则是mp3。请阅读代码，做好音频格式兼容
        - 追加：配置文件的 ocr.model、tts.model 改名为 provider，对应的代码+注释+文档等也要同步修改
        - 追加：调试日志，适当精简阿里千问3-TTS-Flash的日志，如请求Body、URL地址等

[260309]
- Harness
    - 消息：信息输出使用固定结构
        - 任务概述，分类展示信息。`本次任务类型：修改代码，采用多 Agent 架构。` => `任务类型：修改代码；调度架构：多Agent`
        - 迭代功能，Phase未严格遵循命名。`Phase 1: 读取 Skill 流程定义和相关上下文` => `Phase 1: 任务调度`
        - 追加：`同一 Task 内第 2 次反馈`，这个表述非常好，请加入到标准规范中
        - 追加：本次迭代任务，`Phase N：X` 展示格式很好，请固化为 消息输出的格式规则（后续还会补充其它规则）
    - 任务：参考Skill 迭代功能、抽象Skill 迭代其它，主要差异是`Phase 4: 代码实现、Phase 5: 结果验收`合并为`Phase 4: 任务实现`
    - 验收：AI输出信息 `构建通过，唯一 warning 是 xcodebuild 工具级别的 destination 匹配提示（非代码警告），零代码警告` => 必须消除所有警告，请修改规则文档、避免再犯

- 优化
    - 消息：制作历史，最新的的制作记录未展示。期望的效果是，新做的记录、能够展示制作历史
        - 追加：运行一次性任务，回溯补齐部分最新记录的制作历史
    - 消息：制作历史、播放历史，去掉独立的导入导出顶导按钮（随记录一起导入导出），去掉相关代码
    - 消息：调试日志，刷新时没有展示出最新的日志
    - 制作：制作时，自动触发后台制作(而不是必须由切换Tab触发)；记录列表的制作状态，展示百分比
    - 首页：记录播放，按钮图标从下载、改为播放
    - 播放：明确禁用后台播放，锁屏/切APP后 音频停止播放
    - 播放：图片播放，相比音频快了1-2秒
    - 播放：耳机有声音、扬声器没声音(让你禁用后台播放，就给用soloAmbient?真操蛋)
- 新增
    - 播放：播放页功能定位升级，从图片全屏展示、升级为播放器。对应的功能调整如下，功能和样式可参考腾讯视频播放器：
        - 页面组成：播放器 = 全屏图片 + 播放控制层，播放控制层在上、替代之前的overlay悬浮层；
        - 播放视角：播放视角调整为横屏(APP则维持竖屏)，用户视角以手机左侧为底部；
        - 图片布局：图片保持竖屏不变，以维持既有的拍摄和制作效果；动画方向改为自右向左，滑动手势禁用；
        - 播放控制：播放控制层使用横屏，单击控制层切换显示、隐藏，`暂停/继续` 按钮居于控制层左下角；非按钮操作维持不变，如单击、双击、Siri控制等；
        - 新增功能：新增进度条，居播放控制层左下角的操作按钮上方，显示已播放时长/总时长（如 `1:23 / 5:40`），进度条上标注每页音频的分割点（小圆点），暂停状态下支持拖动进度条、在分割点间跳转；
        - 新增功能：新增播放设置框，居播放控制层右上角，随控制层展示/隐藏，标题是`播放设置`，按钮区包括`护眼模式`(默认开启)、`撑满全屏`(默认开启)、`关闭`；播放设置新增`动画样式`控制按钮，支持从右到左(默认选中)、从上到下两种模式(相对横屏控制层)；
        - 功能调整：滑动手势，从禁用改回为支持，暂停状态下可滑动，受`播放设置-动画样式`管控；图片动画应和滑动手势保持一致

[260310]
- 优化
    - 记录：记录列表，embedded模式只展示播放按钮、不展示更多按钮
    - 记录：默认绘本，除播放外、不允许任何其它操作，也不允许出现在播放、制作历史中
- 新增
    - 发布：上架到 AppStore
        - 图标：相机图标侵权，APP图标更换为有声读物(已实现)；隐私条款、应用主页、README等Github公开文档，也都需要更换
        - 隐私：增加隐私条款链接，`隐私条款`放到关于页面的底部
        - 证书：开发证书、发布证书
        - 截图：视觉素材截图，适配设备尺寸
        - 版本：MARKETING_VERSION、CURRENT_PROJECT_VERSION 均使用`Resources/changelogs.md`的最高版本号，其中 MARKETING_VERSION=`主版本.次版本`，CURRENT_PROJECT_VERSION=`主版本.次版本.${自增整数}`。要求自动更新
        - 脱敏：打包文件移除config_local.json


[待办]
- 优化
    - 治理：统一术语，特别是合规的输入
- 新增
    - 播放：图片生视频，使绘本故事更具视觉连续性
