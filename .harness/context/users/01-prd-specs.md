# 产品需求 - 迭代演进

## 约束
本文仅供自然人使用，未经人工确认、禁止AI修改。

## 需求列表
[260304]
- 优化
    - 消息：调试日志，简化内容、提升交互
        - 简化内容，干掉Apple系统日志的原始结构化输出片段 `OSLOG-AA9F2DF2-B46D-4921-94CC-291C9ED07AF9 7 80 L 1a {t:1772585718.989001,tid:0x28ab75}`
        - 简化内容，修改时间头，如从`[2026-02-19 16:24:56.046]` 改为`0219T16:24:56`
        - 提升交互，调试日志改为时间倒序，最新的日志放到最前面
        - 提升交互，支持选中特定文字、复制到剪切板；仅选中所有文本不满足需求，需要支持选中特定文本
    - 我的：APP支持简单的身份名称，如设备名称iPhone，允许自定义修改；要保持APP的工具属性
    - 消息：制作、播放历史，在保证UI功能不变的情况下，存储方式从单独存储、改为合并到会话记录(从而可以随图片&音频一起导入导出)，单次的制作、播放记录增加发起者的身份信息
        - 追加：一次性执行，给每个会话记录补全制作历史，制作时间使用会话的更新时间、使用当前身份
        - 追加：一次性执行，将现有的播放历史，补充到会话记录中
        - 追加：一次性执行，删除单独存放的制作、播放历史信息
        - 追加：清理一次性执行类的任务
        - 追加：getAllSessionMetadata添加caller，识别调用方
    - 播放：播放页面，图片左右滑动后，音频应该调整到当前图片对应的位置
    - 播放：增加防御，任意时刻只允许一个记录播放，如有其它记录在播放、不允许触发新的播放
- 新增
    - 播放：播放时，支持双击播放页面、切换暂停/播放状态
        - 追加：大图左右滑动，播放时禁止、暂停时允许；增加简单的动画效果，使左右滑动更平滑
    - 播放：播放时，支持通过Siri切换暂停/继续，支持通过Siri控制音量
        - 追加：播放时，支持通过Siri 退出播放，语音指令是`退出播放`（结果未达成，系Siri系统限制）
        - 问题：Siri控制暂停/继续播放时，会弹出"正在播放"进度条、影响体验，帮禁用之（结果未达成，系Siri系统限制）
    - 制作：制作时，允许制作转为后台操作，从而使APP能做其它事情(如播放已经存在的会话记录)。
        - 这就需要一个制作记录，方便用户找回后台制作，进而查看制作结果、保存记录等；
        - 制作记录，希望能够复用会话记录，既能简化产品功能，还能拍照后生成记录、防止图片遗失；
        - 制作复用会话记录，制作过程中要 补全分类(如制作中)、补全元信息(如记录名称`YY.MM.DD 未命名`)，制作结束后也要同步更新元数据，最终的保存操作可能仅仅是改一下记录名称
        - 其它说明：只允许1个后台制作任务；不允许修改现有制作页面的UI，处理状态展示也尽量复用制作页
        - 追加：后台制作，制作完成后、不要自动播放，防止重装当前的操作；普通制作维持自动播放

[260306]
- 优化
    - 执行Skill治理巡检时，因上下文过大、AGENT舍弃了`AGENTS.md校正`子任务。通过提示词，要求AGENT自动压缩上下文，确保任务被完整执行。其它Skill，会有类似问题吗？怎样预防自动化进程被中断（已补齐多步任务上下文管理）
    - 执行Skill提取-Harness模板时，子代理结果被截断了，Agent需要读取完整文件内容。请修改Skill描述，避免该问题
- 新增
    - 多Agent：当前项目是单个主Agent，我希望：不同功能分别由专属Agent完成。如，整体调度、理解意图、实现代码、测试验收。请设计下调度架构



[待办]
- 优化
    - 记录：记录列表，分页懒加载、每页最多20个，需要覆盖到首页、记录管理等所有记录列表页
- 新增
    - 记录：记录列表，支持搜索
    - 制作：TTS，配置文件支持选择豆包之外的TTS服务
    - 制作：OCR，配置文件支持选择豆包之外的OCR模型
    - 整体：上架到AppStore
    - 播放：图片生视频，使绘本故事更具视觉连续性
